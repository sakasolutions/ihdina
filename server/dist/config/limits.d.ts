/**
 * Extra-KI-Erklärungen pro UTC-Kalendertag für Free-Nutzer (ohne Tagesvers).
 * Tagesvers zählt nicht → siehe explain.service.
 *
 * Bei Änderung: Entitlement-Response und ggf. App-Fehlermeldung anpassen
 * (Flutter: `explanation_bottom_sheet.dart` Textkonstante).
 */
export declare const MAX_FREE_EXTRA_EXPLANATIONS_PER_DAY = 10;
